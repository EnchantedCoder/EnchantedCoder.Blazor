@attribute [Route("/components/" + nameof(EcFormState))]

<ComponentApiDoc Type="typeof(EcFormState)">

	<p>Publishes <code>@nameof(FormState)</code> as cascading value. Allows you to enable/disable child components by one property.</p>

	<DocHeading Title="Demo" />
	<Demo Type="typeof(EcFormState_Demo)" />

</ComponentApiDoc>