@attribute [Route("/components/" + nameof(EcAnchorFragmentNavigation))]

<ComponentApiDoc Type="typeof(EcAnchorFragmentNavigation)">

	<DocAlert Type="DocAlertType.Warning">
		As Blazor uses the <code>&lt;base&gt;</code> element, all the links have to include full page path!
	</DocAlert>

	<DocHeading Title="Basic usage" />
	<Demo Type="typeof(EcAnchorFragmentNavigation_Demo)" />
</ComponentApiDoc>