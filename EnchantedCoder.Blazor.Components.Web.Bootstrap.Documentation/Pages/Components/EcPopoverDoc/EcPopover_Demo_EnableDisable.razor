<EcPopover @ref="popover1" Title="Popover title" Content="And here's some amazing content. It's very engaging. Right?">
	<EcButton Color="ThemeColor.Primary">Click to toggle popover</EcButton>
</EcPopover>
<EcButton Text="Disable" OnClick="HandleDisableClick" Color="ThemeColor.Warning" Outline="@(!enabled)" Spinner="false" />
<EcButton Text="Enable" OnClick="HandleEnableClick" Color="ThemeColor.Success" Outline="@enabled" Spinner="false" />

@code
{
	private EcPopover popover1;
	private bool enabled = true;

	private async Task HandleEnableClick()
	{
		await popover1.EnableAsync();
		enabled = true;
	}

	private async Task HandleDisableClick()
	{
		await popover1.DisableAsync();
		enabled = false;
	}
}