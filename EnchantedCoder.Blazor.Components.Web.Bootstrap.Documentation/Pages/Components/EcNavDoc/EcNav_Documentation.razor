@attribute [Route("/components/" + nameof(EcNav))]
@attribute [Route("/components/" + nameof(EcNavLink))]

<ComponentApiDoc Type="typeof(EcNav)">
	<DocHeading Title="Base Nav" />
	<Demo Type="typeof(EcNav_Demo_BaseNav)" />

    <DocHeading Title="Vertical" />
	<Demo Type="typeof(EcNav_Demo_Vertical)" />

    <DocHeading Title="Tabs" />
	<p>The raw implementation requires you to set the <code>active</code> class on your own (the <code>Match</code> behavior on <code>Href</code> might help).</p>
	<p>See <a href="/components/@nameof(EcTabPanel)">@nameof(EcTabPanel)</a> for more sophisticated implementation.</p>
	<Demo Type="typeof(EcNav_Demo_Tabs)" />

	<DocHeading Title="Underline" />
	<p>Use <code>Variant="NavVariant.Underline"</code> for underlined style:</p>
	<Demo Type="typeof(EcNav_Demo_Underline)" />

    <DocHeading Title="Pills" />
	<Demo Type="typeof(EcNav_Demo_Pills)" />

    <DocHeading Title="Dropdowns" />
	<Demo Type="typeof(EcNav_Demo_Dropdowns)" />
</ComponentApiDoc>

<ComponentApiDoc Type="typeof(EcNavLink)" />
